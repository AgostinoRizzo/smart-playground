#ifndef SENSING_H
#define SENSING_H

#define SAMPLING_PERIOD           1000  // sensing period (millis)
#define SAMPLES_BUFFER_CAPACITY     10  // max number of buffered samples

// sensors value type
typedef uint16_t sensor_value_t;

// buffer size type
typedef uint8_t buffer_size_t;

#endif
